# Project context: Even G2 × 서울 지하철 플러그인 (metro)

참고 프로젝트: `../tiro` (같은 G2 플러그인). G2 SDK 제약과 실기기 검증 결과는
`../tiro/.agent/context.md`에 있다. 새 제약만 여기 적는다.

## 2026-09-20 목표와 기준 (사용자 확인)

- 핵심 장면은 "탑승 중 동행"이다. 열차를 탄 뒤가 주전장이다. 남은 정거장, 환승 시점, 하차 시점을 안내한다.
- 입력 방식은 "폰에 경로 저장 → G2에서 선택"이다. 경로 탐색은 1차 범위 밖이다.
- 기존 앱의 불만 4가지를 모두 받았다: 조작이 번거롭다, 정보가 정적이다, 하차/환승을 못 챙긴다, 화면이 지저분하다.
- 사용자 원문: "텍스트로만 이루어져있는데, 단순 줄바꿈으로만 구분되어있어서 정보의 위계도 없고, '그냥 정보 나열해놨으니 니가 봐라'라는듯한 태도가 마음에 들지 않아"
- 따라서 합격 기준은 두 가지다. (1) 화면에 정보 위계가 있다. (2) 앱이 "지금 알아야 할 한 가지"를 먼저 말한다.

## 2026-09-20 서울 지하철 API 실측 (curl)

### 실시간 열차 위치 — `swopenapi.seoul.go.kr`
- `GET http://swopenapi.seoul.go.kr/api/subway/{KEY}/json/realtimePosition/0/{N}/{노선명}`
- 200 정상. `Access-Control-Allow-Origin: *`. preflight(OPTIONS)도 200이다. → WebView에서 직접 호출한다. 프록시가 필요 없다.
- **HTTPS는 응답하지 않는다(타임아웃).** http 전용이다. 이것이 최대 위험이다. 아래 "미검증" 참고.
- 응답 필드: `trainNo`(열차번호), `statnNm`(현재 역), `updnLine`(0=상행/외선, 1=하행/내선), `trainSttus`(0=진입, 1=도착, 2=출발), `statnTnm`(종착역), `directAt`(급행), `recptnDt`(초 단위 시각).
- 실측: 열차 `2301`이 약 1분 사이에 잠실새내 → 잠실로 이동했다. **`trainNo`로 특정 열차를 계속 추적할 수 있다.**
- 노선 커버리지 확인: 2호선 37대, 9호선 26대, 신분당선 12대, 수인분당선 32대, 경의중앙선 23대. 코레일·민자 노선도 나온다.

### 실시간 도착 정보 — 같은 도메인
- `GET .../json/realtimeStationArrival/0/{N}/{역명}`
- `btrainNo`(도착 예정 열차번호), `barvlDt`(도착까지 남은 초), `arvlMsg2`(예: "서울 출발"), `trainLineNm`(예: "광운대행 - 시청방면"), `btrainSttus`(일반/급행)를 준다.
- 용도: 승강장에서 탑승할 열차의 `trainNo`를 미리 잡는다.

### 역 목록 — `openapi.seoul.go.kr:8088`
- `GET http://openapi.seoul.go.kr:8088/{KEY}/json/SearchSTNBySubwayLineInfo/1/800/`
- 799개 역. 필드 `STATION_CD`, `STATION_NM`, `LINE_NUM`("01호선"), `FR_CODE`("P148", "151").
- `FR_CODE`의 숫자 부분이 노선 내 역 순서다. 환승역은 같은 `STATION_NM`이 여러 `LINE_NUM`에 나온다.
- CORS `*`. 역시 http 전용이다.
- **빌드 시점에 한 번 받아 정적 JSON으로 번들한다.** 런타임 의존을 만들지 않는다.
- `SearchSTNTimeByFRCodeService`(역간 소요시간)는 `ERROR-500`이다. 쓸 수 없다.

### API key
- `sample` key는 5건만 준다. 100건 요청은 `ERROR-336`으로 거부된다(오류 문구는 건수 한도를 말하지만 실제 원인은 sample 제한이다).
- 실제 key는 data.seoul.go.kr에서 무료로 발급한다. tiro와 같은 BYOK로 간다.

## 2026-09-20 미검증 (착수 전 확인 필요)

1. **`.ehpk` private build의 페이지 scheme.** 페이지가 https면 http API 호출이 mixed content로 차단된다. QR sideload(`http://<ip>:5173`)는 http라서 문제가 없다. 1차는 sideload로 만들고, 배포 시점에 https 프록시 필요 여부를 판단한다.
2. **G2 폰트 크기는 조절할 수 없다**(tiro 확인). 큰 글씨로 위계를 만들 수 없다. 여백, 정렬, 컨테이너 분할, 문자 그래픽으로 만들어야 한다. 이미지 컨테이너(288x144, 페이지당 4개) 렌더링은 마지막 수단이다.
3. 코레일 직결 구간(1·3·4호선, 경의중앙선 등)에서 `trainNo`가 중간에 바뀌는지 확인이 필요하다.
4. 지하 구간에서 폰 네트워크가 끊기는 빈도. polling 실패 시 동작을 정해야 한다.

## 2026-09-20 API key 조사

- `sample` 키의 한도는 `openapi.seoul.go.kr:8088`에서 **1~5번 항목 고정**이다. 오프셋을 옮겨도 `ERROR-335`다. 그래서 `stations.json`을 만들려면 실제 키가 필요하다. `swopenapi` 쪽 `sample` 한도도 5건이다.
- 데이터셋 이름 주의: "서울시 지하철역 정보"라는 이름은 열린데이터광장 목록에 없다. 서비스 이름 `SearchSTNBySubwayLineInfo`는 실재하고 799개역을 준다.
- "지하철역 최단경로이동정보 현황"은 역 구내 이동/환승 통로 정보다. 노선 내 역 순서가 아니다. 1차 범위에는 쓰지 않는다. 나중에 "빠른 환승 칸" 기능의 후보다.
- 미검증: 열린데이터광장 인증키 하나가 `openapi.seoul.go.kr:8088`과 `swopenapi.seoul.go.kr` 양쪽에 모두 통하는지. 키를 받는 즉시 두 도메인을 함께 시험한다.
- 대안: 8088이 거부하면 열린데이터광장의 CSV 파일 내려받기(`FILE` 표시 데이터셋)로 역 목록을 얻는다.

## 2026-09-20 키 실측: 두 개가 필요하다

- 열린데이터광장 **일반 인증키**는 `openapi.seoul.go.kr:8088`에서만 통한다. `swopenapi.seoul.go.kr`에 쓰면 `ERROR-338 해당 인증키로는 실시간 서비스를 사용할 수 없습니다`다.
- 실시간 지하철 API는 별도 신청이다. `.env.local`에 `SEOUL_KEY`(일반)와 `SEOUL_RT_KEY`(실시간)를 나눠 둔다.
- 일반 키로 `SearchSTNBySubwayLineInfo` 799개역 전량을 받았다.

## 2026-09-20 FR_CODE 구조와 그래프 경로 탐색

- `FR_CODE`의 숫자만 뽑으면 안 된다. `P142`(가산디지털단지)와 `142`(구일)가 충돌한다. 1호선 20건, 5호선 7건, 경의선 2건이 겹쳤다.
- 세 꼴이다. `NNN` 본선, `XNNN` 알파벳 분기(1호선 경부선, 5호선 마천지선), `NNN-M` 지선(2호선 성수지선은 `211-*`, 신정지선은 `234-*`).
- `{ branch, order }`로 쪼개면 `line + branch + order`가 유일하다. 빌드 스크립트가 중복을 검사하고 멈춘다.
- **순서 산술 대신 인접 그래프 BFS를 쓴다.** 간선 규칙 4개로 순환선·지선·분기가 한 번에 풀린다. 노드 799, 간선 775, 고립 0.
- 검증: 2호선 충정로→시청 1정거장(순환 닫힘), 강남→신촌 18(짧은 쪽), 1호선 서울역→수원 22(경부선 분기 건넘), 5호선 광화문→마천 22(마천지선), 2호선 성수→신설동 4(성수지선).

## 2026-09-20 좌표 데이터와 GPS 지원 확인

- `GET http://openapi.seoul.go.kr:8088/{KEY}/json/subwayStationMaster/1/1000/`가 `BLDN_NM`, `ROUTE`, `LAT`, `LOT`를 준다. 784개역이다.
- 역 목록의 고유 역 이름 655개 중 647개가 매칭된다. 안 되는 8개는 최근 개통역이다(운정중앙, 평택지제, 시우 등). 직접 고르기로 넘긴다.
- **G2 SDK가 위치를 정식 지원한다.** `bridge.getAppLocation()`, `startAppLocationUpdates()`, `stopAppLocationUpdates()`, `onAppLocationChanged()`. 값은 `{latitude, longitude, accuracy, altitude, speed, heading, timestamp}`이고 `AppLocationAccuracy`는 low/medium/high다.
- `app.json`이 허용하는 권한 전체(CLI 스키마 실측): `g2-microphone`, `phone-microphone`, `album`, `location`, `network`, `camera`.

## 2026-09-20 환승 포함 전체 경로 탐색 검증

- 같은 이름 다른 노선을 환승 간선으로 잇고(176개), 환승 1회를 5정거장으로 가중해 다익스트라를 돌렸다.
- 결과가 실제 추천 경로와 일치한다. 잠실→경복궁(2호선·을지로3가·3호선), 서울역→강남(4호선·사당·2호선), 노원→여의도(4호선·동대문역사문화공원·5호선), 수원→강남(수인분당선·정자·신분당선).
- 한계: 이 그래프는 급행과 직결 운행 패턴을 모른다. 정거장 수 기준 최단이지 소요시간 최단이 아니다. 환승 가중치 5는 조정 손잡이다.

## 2026-09-20 실시간 키 실측: updnLine을 쓰지 말 것

- **두 API의 `updnLine` 표기가 다르고, 노선마다 극성이 반대다.** 같은 열차번호로 교차 검증했다.
  `상행 → 0`(34건), `하행 → 1`(31건), `외선 → 1`(14건), `내선 → 0`(14건). 4%는 어느 쪽에도 안 맞았다(회차 열차 추정).
- `order` 증가가 어느 방향인지도 노선마다 다르다. 1·3·4·5·6·7·8호선, 신분당선, 수인분당선은 `order` 증가 = 하행(1)이다. **2호선은 `order` 증가 = 내선(0), 9호선은 `order` 증가 = 상행(0)으로 반대다.**
- **해법: `updnLine`을 읽지 않는다.** `trainLineNm`의 `"…방면"` 역이 경로의 다음 역(`stops[1]`)과 같은지로 방향을 가른다. 극성 표가 통째로 필요 없어졌다. `direction()` 함수와 `updn` 필드를 지웠다.
- 근거: `"…방면"`은 급행이든 일반이든 **물리적으로 인접한 다음 역**이다. 9호선 급행 `9641`이 여의도에서 `국회의사당방면`, 노량진에서 `샛강방면`으로 나왔다. 둘 다 인접역이다.
- `trainLineNm` 형식은 `"{종착역}행 - {다음역}방면"`이고 급행이면 `" (급행)"`이 붙는다. 201건 전부 이 꼴이었다.
- 파싱 주의: `$` 앵커를 쓰면 `(급행)` 꼬리에서 실패한다. 괄호 별칭(`총신대입구(이수)방면`)도 떼야 역 목록과 맞는다. 둘을 처리하면 201건 중 201건 파싱, 199건이 역 목록과 일치한다.
- 실측 필터 결과: 2호선 강남 교대방면 4대 중 2대, 1호선 시청 종각방면 8대 중 4대. 정확히 절반으로 갈린다.

## 2026-09-20 노선명 별칭과 실시간 미지원 노선

- 역 목록과 실시간 API의 노선명이 다르다. 실시간 API에 후보 이름을 직접 던져 확인했다. 추측하지 않았다.
  - `경의선` → `경의중앙선` (id 1063)
  - `우이신설경전철` → `우이신설선` (id 1092)
- **실시간 API가 지원하지 않는 노선 5개**: 김포도시철도, 용인경전철, 의정부경전철, 인천선, 인천2호선. 역 100개다. 서울시 TOPIS 데이터라 인천·김포·용인·의정부 자체 노선이 없다.
- 이 노선들을 그래프에서 뺀다. 남겨 두면 경로가 추적 불가 구간을 지나고 안내가 조용히 멈춘다.
- 노선 ID 19개를 받았다. 역 699/799개가 추적 가능하다.
- 빌드 스크립트가 주요 노선(1~9호선) ID가 빠지면 멈춘다. 막차 뒤에 돌리면 `lines`가 비어 버리기 때문이다.

## 2026-09-20 저장소

- GitHub: https://github.com/HC-kang/G2P-for-metro (`HC-kang`)
- 워커는 커스텀 도메인 없이 기본 `*.workers.dev`를 쓴다.

## 2026-09-20 실수와 원인: HMR이 polling 루프를 쌓았다

- 증상: "7283번 열차를 찾지 못했습니다"가 반복해서 떴다. 로그에 `poll failed` 2,335회, `train not in feed` 512회가 찍혔다. `show`가 10초 주기보다 훨씬 잦았다.
- 원인: Vite HMR은 모듈을 다시 실행한다. `main.ts`가 `setTimeout` 루프와 `onEvenHubEvent` 구독을 정리하지 않아, 편집할 때마다 인스턴스가 하나씩 쌓였다. 옛 인스턴스는 `trip`이 `null`이라 `leg()`에서 예외가 났고 그것이 `poll failed`로 찍혔다. 살아남은 루프들이 같은 화면을 서로 덮어썼다.
- 확인: 7283은 위치 API에 실제로 있었다(도봉산). 노원 도착 예정 4건 중 위치 API에 없는 것은 0건이었다. 워커도 12/12 정상(0.47초)이었다. 즉 API 문제가 아니었다.
- 수정 3가지:
  1. `import.meta.hot.dispose`에서 `stopPolling()`과 `unsubscribe()`를 부른다.
  2. polling에 세대 번호를 둔다. `stopPolling()`이 세대를 올리고, 옛 루프는 자기 세대가 아니면 아무것도 하지 않는다. `await` 뒤에도 세대를 다시 확인한다.
  3. 한 번 놓친 것으로 오류를 띄우지 않는다. 3회 연속(30초) 실패해야 오류 화면을 낸다.
- 교훈: 실기기 디버깅에서 HMR은 상태를 가진 코드의 증상을 통째로 왜곡한다. 타이머와 구독을 쓰면 `dispose`를 먼저 붙인다.

## 2026-09-20 G2 화면 폭

- **전각 문자를 세지 않으면 화면이 조용히 접힌다.** `─` 34개는 68칸이다. 진행 띠의 `━` 구분자도 12개를 이으면 46칸이다.
- `cols()`로 칸을 센다(한글·도형 2칸, 그 밖 1칸). `MAX_COLS = 32`는 아직 추측값이다. 실측 범위는 "32칸은 안전, 68칸은 넘침"이다.
- 긴 역 이름(`동대문역사문화공원` 18칸)은 자간을 벌리면 넘친다. `hero()`가 넘치면 자간을 포기한다. `pair()`가 넘치면 줄을 나눈다.
- 머리줄은 `현재시각 HH:MM`이다. 화면에 도착 예정 시각도 나오므로 현재 시각임을 밝혀야 한다(사용자 지적).

## 2026-09-21 역 이름 표기가 API마다 다르다 (중대)

- **도착 API는 자기 표기로만 역을 받는다.** `/arrival/공릉`은 `INFO-200 해당하는 데이터가 없습니다`, `/arrival/공릉(서울산업대입구)`는 정상이다.
- 표기가 세 갈래다. 괄호 붙은 역 67개를 실제로 두드려 확인했다.
  - 짧은 이름으로 되는 역 38개
  - 괄호 이름이 필요한 역 20개 (`총신대입구(이수)`, `군자(능동)`, `몽촌토성(평화의문)` 등)
  - 제3의 별칭을 쓰는 역 9개. `공릉`은 역 목록도 좌표 데이터도 `공릉(서울과학기술대)`라 하는데 도착 API는 `공릉(서울산업대입구)`다. **좌표 데이터(subwayStationMaster)를 별칭 출처로 쓰면 안 된다.**
- **위치 API는 또 다르다.** `realtimePosition`의 `statnNm`이 `군자(능동)`처럼 괄호를 달고 온다. 역 목록은 `군자`다. 비교할 때 괄호를 떼지 않으면 그 역에서 관측이 끊겨 현위치가 고정된다.
- 해결:
  1. 빌드 때 도착 API를 훑어 `arrivalNames` 표를 만든다. 정식 표기는 이웃 역 응답의 `trainLineNm`의 `…방면`에서 줍는다. 550/566개를 확인했고 별칭 20개를 찾았다.
  2. 런타임 비교는 `bare()`로 양쪽 괄호를 떼고 한다.
  3. 표는 **병합**한다. 막차 뒤에 돌리면 도착 열차가 없어 대부분 실패하는데, 그때 표를 잃으면 안 된다. 실행할 때마다 아는 것이 늘기만 한다.
- 주의: 이 훑기는 "이름이 틀림"과 "지금 열차가 없음"을 구별하지 못한다. 반드시 운행 시간대에 돌린다.

## 2026-09-21 사용자 지적 3가지

- "공릉 찍었는데 도착 예정인 지하철이 없다" → 위 별칭 문제였다.
- "태릉입구에 왔는데 현위치가 하계로 나온다" → 괄호 표기 불일치로 관측이 끊긴 것으로 본다. `bare()` 비교로 고쳤다.
- "언제 폴링되는지 표시가 필요하다" → 화면에 `노원 출발  8초 전`처럼 데이터 나이를 보여준다. 기준은 `recptnDt`(API 보고 시각)이므로 API 지연까지 포함한다.
- 열차 목록이 행선지만 보여줬다. 승강장에서 확인할 것은 **방면**이므로 `3분  공릉방면  석남행`으로 바꿨다.

## 2026-09-21 서울 실시간 API는 하루 1000건이 한도다 (중대)

- `ERROR-337 데이터요청은 일일 호출건수 최대 1000건을 넘을 수 없습니다.`
- **오류도 HTTP 200에 본문으로 온다.** 목록이 없다고 빈 배열로 넘기면 "도착 정보가 없습니다"로 둔갑해 원인을 숨긴다. `ApiError`로 던지고 화면에 그대로 말한다.
- `INFO-200`은 오류가 아니다. "그 역에 올 열차가 지금 없다"는 뜻이다(막차 뒤에 흔하다).
- 10초 고정 폴링은 시간당 360건이다. 두 번만 타면 바닥난다. 빌드 훑기도 566건을 태웠다.
- 해결: 상황별 폴링. 하차 2정거장 이내 15초, 열차 대기 20초, 그 밖 35초. 워커 캐시 20초.
  실측 예산: 21정거장 왕복 2회에 194건(이전 방식이었다면 600건).
- 빌드 스크립트는 이미 아는 것을 **병합**한다. 한도를 쓴 날에는 조회가 전부 실패하므로 덮어쓰면 표를 잃는다.

## 2026-09-21 그래프의 가짜 환승과 끊긴 지선

- **이름만 같고 환승이 안 되는 역이 있다.** `subwayStationMaster`가 노선별 좌표를 주므로 같은 이름의 최대 거리로 가려낸다. 600m를 경계로 쓴다.
  - 별개 역: 양평 53.5km(중앙선/5호선), 운정 3.6km(경의중앙선/GTX-A), 신촌 715m(2호선/경의중앙선)
  - 진짜 환승: 서울역 510m, 잠실 401m, 동대문역사문화공원 343m, 청량리 339m, 신당 311m
  - 빌드 때 계산해 `noTransfer`로 저장한다. 손으로 적지 않는다.
- 미해결: 경의중앙선 서울역 지선(P312 신촌, P313 서울역)이 본선과 끊겨 있다. 실제로는 가좌에서 분기하는데 `FR_CODE`에 숫자 규칙이 없다(P312 ↔ K315). 지선 운행이 하루 몇 편뿐이라 틀린 간선을 만드는 것보다 끊어 두는 편이 낫다고 보았다. 서울역은 다른 노선으로 도달한다.

## 2026-09-21 출발 노선 선택

- 사용자 지적: "최단 환승 정보라지만 호선을 지맘대로 고르면 어떡하냐."
- `planVia(from, to, line)`를 추가했다. 출발역에서 곧장 환승하는 것을 막으려고 **그 노선의 이웃 역에서 다익스트라를 시작**한다. 출발역 노드에서 시작하면 바로 환승해 다른 노선 경로가 나온다.
- 한 정거장 나갔다 되돌아오는 경로는 버린다(`legs[0].stops.at(-1) === from`).
- 선택지가 하나뿐이면 화면을 띄우지 않는다. 탭이 늘지 않는다.

## 2026-09-21 같은 유형 훑기 (사용자 요청)

사용자가 하루 동안 지적한 것을 유형으로 정리하고 같은 유형을 코드 전체에서 찾았다.

| 유형 | 처음 지적 | 같은 유형으로 더 찾은 것 |
|---|---|---|
| 재보지 않고 정한 상수 | `─` 34개(68칸), 도착지 8개 한도 | 10초 폴링이 하루 한도 1000건을 넘김 |
| 오류를 삼켜 엉뚱한 메시지 | `ERROR-337` → "도착 정보 없음" | 목록 항목을 글자 중간에서 자름(`온수행 급행` → `온수행 급`) |
| 화면에 꼭 필요한 것이 없음 | 현재 역, 현재시각, 갱신 시각 | 환승 화면의 방면이 종착역이라 승강장 표지와 기준이 다름 |
| 데이터를 조용히 버림 | GPS 오차 1km 초과, 괄호 역 이름 | 경로가 없는 도착지를 목록에서 숨김 |
| 사용자가 정할 것을 내가 정함 | 출발 호선 | (없음) |

고친 것:
- `tiers()`로 목록 곁가지를 단계적으로 줄인다. 자르지 않는다.
- 경로가 없는 도착지도 목록에 두고 `경로 없음`이라고 적는다. 숨기면 저장이 안 된 줄 안다.
- 환승 화면의 방면을 환승 뒤 **다음 역**으로 바꿨다. 승강장 표지와 같은 기준이다.
- 조회량을 직접 세어 800건을 넘으면 출발 화면과 경로 화면에 보여준다. 날짜가 바뀌면 0이 된다.
- `toMs`가 서버 시각과 10분 넘게 어긋나면 파싱을 믿지 않고 받은 시각을 쓴다. 기기 시간대가 KST가 아니면 "3시간 전" 같은 거짓말이 나온다.
- 문구를 고쳤다. `선릉 이후 190초` → `마지막 관측  선릉  3분 전`.

남긴 추측값(근거를 못 잰 것, 주석에 명시):
- `MAX_COLS = 32` — 실측 범위는 "32칸 안전, 68칸 넘침"이다. 그 사이를 못 쟀다.
- `DEFAULT_PACE_MS = 120000` — 관측 2건 미만일 때만 쓴다.
- `TRANSFER_MIN = 4` — 경로 순위에 영향을 준다. 화면에는 '약'을 붙인다.

## 2026-09-21 출발 노선 선택 검증 (실데이터)

노선 2개 이상인 역 110곳 × 목적지 15곳 = 경로 1,637개로 확인했다.

- **결함: 최선 경로가 선택지에서 빠지는 경우가 10건 있었다.** `planVia`는 그 노선으로 한 정거장을 먼저 타게 강제하는데, 그 이웃에서 다시 탐색하면 다른 노선이 최적이 되어 걸러졌다. → `plan()`의 최선을 **항상 먼저 넣는다.** 유실 0이 됐다.
- 쓸모없는 선택지가 142건이었다(환승 3~4회, 최선의 2배 시간). 규칙을 실측으로 정했다.
  `환승 +1 이내 & 최선 +15분 이내`, 단 최선은 무조건 남긴다. → 쓸모없는 것 131 → 46, 최선 유실 0.
- 구조 검증 3,384개 경로: `stops[1]` 없음 0, 이웃 아님 0, 환승역 연결 깨짐 0.
- 실제 결과 예: 왕십리→강남은 수인분당선(7정거장·환승1·18분), 경의중앙선(7·환승2·22분), 2호선(14·직통·28분) 셋이 나온다. 홍대입구→서울역은 공항철도 직통과 경의중앙선 둘이다.

## 2026-09-21 도착 API 이름 예비 후보

- 훑기로 550/566을 확인했고 16개가 남았다. 그중 실제로 쓰는 역은 응암, 이수, 아차산, 광나루, 새절, 자양이다.
- 대비책 3가지.
  1. 좌표표(subwayStationMaster)의 괄호 이름 67개를 `altNames`로 싣는다. API 호출이 들지 않는다.
  2. 런타임에서 조회가 비면 예비 이름으로 **한 번만** 더 부른다. 한도가 아까우므로 실패할 때만 쓴다.
  3. 가운뎃점을 마침표로 바꾼 변형도 시도한다(`4·19민주묘지` → `4.19민주묘지`).
- 훑기가 위치 API의 `statnNm`도 줍도록 했다(노선당 1건). 운행 시간대에 다시 돌리면 남은 16개가 줄어든다.

## 2026-09-21 로그를 놓치지 않는 방법

문제: `wrangler tail`은 실시간 스트림이다. 붙어 있지 않으면 로그가 사라진다. 오늘 두 번 잃었다.
dev server 로그(`/__log`)는 같은 Wi-Fi에서만 받으므로 지하철에 타면 끊긴다.

저장소(KV/D1) 없이 쓰는 방법 세 가지를 겹쳐 둔다.

1. **Cloudflare Workers Logs** — `wrangler.toml`에 `[observability] enabled = true`.
   바인딩이 필요 없다. 대시보드에서 나중에 조회된다. 이것이 "저장소 안 쓰고 보존"의 답이다.
2. **앱 안 링 버퍼** — 최근 120줄을 메모리에 둔다. 폰 설정 화면의 `진단 기록`에서 바로 읽는다.
   내가 붙어 있지 않아도 사용자가 그 자리에서 본다.
3. **지난 실행 보존** — `error`/`unhandledrejection`에서 버퍼를 `bridge.setLocalStorage('trail')`에 남긴다.
   화면이 죽기 직전 기록이 가장 쓸모 있는데 메모리에만 두면 함께 사라진다.

`서버로 보내기` 버튼이 머리말(버전, 시각, 오늘 조회량, 도착지 수)과 함께 묶어 보낸다.
워커는 덤프를 **줄 단위로 나눠** `console.log` 한다. 한 줄이 길면 대시보드에서 잘린다.
본문 상한 8,000자, 줄당 600자다.

## 2026-09-22 선택지의 단위는 노선이 아니라 (노선, 방면)이다

- 사용자 지적: "하계에서 홍대입구 갈 때 공릉방향 선택지는 없이 중계행만 나온다."
- 원인: `planVia(from, to, line)`이 노선만 지정했다. 하계에는 7호선뿐이라 양방향이 한 후보로 뭉개졌고 빠른 쪽(중계 방면)만 남았다.
- 수정: `planHop(from, to, line, next)`로 **첫 한 정거장을 지정**한다. `departures(from)`이 출발역의 모든 (노선, 방면)을 준다.
- 함정: 방향만 강제하면 다익스트라가 **출발역으로 되돌아가는 길**을 고른다. 하계에서 공릉 방면을 고르면 "공릉 → 하계 → 중계 → 노원 → 4호선"이 더 짧다고 본다. 사후에 버리면 그 방향의 진짜 경로까지 함께 잃는다. → **탐색 단계에서 출발역의 모든 노드를 막는다**(`search`의 `banned`).
- 결과: 하계→홍대입구가 중계방면 46분(4호선 경유), 공릉방면 50분(7호선 5 ▶ 경의중앙선 4 ▶ 2호선 12) 둘 다 나온다.
- 검증 6,780개 경로: 최선 유실 0, 라벨 중복 0, 역 되밟기 0, 구간 역<2 0. 선택지 2개 이상인 경우 941 → 2,385.

## 2026-09-22 조회수 28,165 — 한도를 넘어도 계속 불렀다

- 사용자 화면에 `오늘조회 28165/1000`이 떴다.
- 코드 확인: 호출 지점은 둘뿐이고 각각 +1이다. 타이머는 하나이고 최소 15초다. 즉 카운터는 맞고, 정말 그만큼 불렀다.
- **가장 유력한 원인은 어제의 HMR 루프 누적이다.** `pollGen`은 모듈 변수라 HMR로 모듈이 다시 실행되면 새 인스턴스가 자기 `pollGen`을 갖는다. 옛 인스턴스의 루프는 자기 기준으로 살아 있다. `import.meta.hot.dispose`를 붙이기 전까지 편집할 때마다 루프가 쌓였다. (단정하지 않는다. 당시 로그가 없다.)
- **확실한 결함: 한도를 넘어도 막지 않았다.** 경고만 하고 계속 불렀다.
- 수정 4가지.
  1. `guard()`가 **호출 전에** 막는다. `used >= 1000`이면 부르지 않는다.
  2. 분당 10건 폭주 차단. 정상은 분당 2~4건이다. 어떤 폭주든 여기서 멈춘다.
  3. 날짜 경계를 KST로 고쳤다. UTC를 쓰면 오전 9시에 초기화된다. 서울 API는 KST 자정에 돈다.
  4. 진단 화면에 `조회수 초기화` 버튼. 잘못 쌓인 값 때문에 앱이 막히면 되돌린다.

## 2026-09-22 고른 경로 기억

- 사용자 요청: "사용자가 도착지만이 아니라 루트도 지정할 수 있게."
- `prefs`에 `출발>도착` → 경로키를 저장한다. 다음에 같은 구간이면 그 선택지를 맨 위로 올리고 `★`를 붙인다. 자동으로 건너뛰지는 않는다. 바꾸고 싶을 때가 있다.

## 2026-09-22 실수와 원인: 초기화 중 ReferenceError로 앱이 반만 살아남

- 증상: 저장된 도착지가 안 보이고, 역을 하나 추가해야 그제서야 보임. 글래스 시작 화면 없음. 보내기·초기화 버튼 무반응.
- 원인: `renderTrail()`을 255줄에서 부르는데, 그 본문이 읽는 `used`·`QUOTA_DAY`·`usedDay`는 299~307줄에서 선언됐다. **TDZ ReferenceError**로 모듈 초기화가 그 줄에서 멈췄다. 앞에 등록된 `#add` 핸들러만 살아서 "추가하면 보이는" 반쪽 상태가 됐다.
- `tsc`는 못 잡는다. 같은 스코프에서 직접 읽는 것만 잡고, 함수 안에서 읽는 것은 잡지 않는다.
- 수정 3가지.
  1. 조회 한도 블록을 설정 UI보다 앞으로 옮겼다.
  2. `main.ts` 맨 위에 어떤 선언에도 기대지 않는 안전망을 달았다. `window.error`/`unhandledrejection`을 폰 화면 맨 위 `#fatal`에 적는다. **초기화가 어디서 터지든 사람이 본다.**
  3. `src/order.test.ts`: 최상위 호출의 함수 본문이 그 아래에서 선언되는 const/let을 읽으면 실패하는 정적 검사. 어제 모양의 사고를 잡는 것을 테스트로 확인했다.
- 교훈: main.ts는 "상태 선언 → 함수 정의 → 최상위 호출" 순서를 지킨다. 최상위에서 곧바로 부르는 코드를 끼워 넣을 때는 그 함수가 읽는 변수가 위에 있는지 본다.
